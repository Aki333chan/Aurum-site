server {
    listen 80;
    listen [::]:80;
    server_name aurumgg.ovh www.aurumgg.ovh;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 503;
    }
}
